{% include "postgresql/server/files/defaults/{0}_postgresql.conf".format(pillar['postgresql']['version']) %}
{% set date = salt['cmd.run']('date') %}

# The following was auto-generated by SaltStack on {{ date }}

include "options.conf"
